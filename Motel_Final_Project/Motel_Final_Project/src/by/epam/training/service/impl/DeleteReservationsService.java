package by.epam.training.service.impl;import by.epam.training.dao.DAOException;import by.epam.training.dao.DAOFactory;import by.epam.training.dao.ReservationDAO;import by.epam.training.service.ServiceException;import java.util.HashMap;/** * Created by Михаил on 07.06.2016. */public class DeleteReservationsService{    private static final DeleteReservationsService instance = new DeleteReservationsService();    private static final String ORDER_ID="orderID";    private static final String ATTR_DELETED="reservations";    private static final String ERROR_MESSAGE="Reservation can't be deleted";    public static DeleteReservationsService getInstance(){        return instance;    }    public void doService(HashMap<String,String> parameters) throws ServiceException {        try {            System.out.println("====================[ DELETE RESERVATION ]=========================");            // read info from request            String orderID = parameters.get(ORDER_ID);            DAOFactory daoFactory = DAOFactory.getDAOFactory();            ReservationDAO reservationDAO = daoFactory.getReservationDAO();           boolean deleteReservation = reservationDAO.deleteReservation(orderID);            if (deleteReservation) {                System.out.println("User has deleted reservation.");                //            } else {                System.out.println(ERROR_MESSAGE);                throw new ServiceException(ERROR_MESSAGE);            }        }        catch (DAOException e) {            throw new ServiceException(e);        }    }}